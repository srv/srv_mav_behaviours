#!/usr/bin/env python
PACKAGE = "srv_mav_behaviours"

from dynamic_reconfigure.parameter_generator_catkin import *

gen = ParameterGenerator()

#gen.add("int_param", int_t, 0, "An Integer parameter", 50, 0, 100)
#gen.add("double_param", double_t, 0, "A double parameter", .5, 0, 1)
#gen.add("str_param", str_t, 0, "A string parameter", "Hello World")
#gen.add("bool_param", bool_t, 0, "A Boolean parameter", True)

#size_enum = gen.enum([ gen.const("Small", int_t, 0, "A small constant"),
#                  gen.const("Medium", int_t, 1, "A medium constant"),
#                  gen.const("Large", int_t, 2, "A large constant"),
#                  gen.const("ExtraLarge", int_t, 3, "An extra large constant") ],
#                  "An enum to set size")

#gen.add("size", int_t, 0, "A size parameter which is edited via an enum", 1, 0, 3, edit_method=size_enum)

gen.add("max_speed", double_t, 0, "Maximum speed allowed", 1.0, 0, 5)

gen.add("min_distance_wall", double_t, 0, "Minimum distance allowed from walls", .5, 0.5, 10.0)
gen.add("attenuation_distance_wall", double_t, 0, "Distance from the wall in meters to start attenuating the speed", .5, 0.5, 10.0)
gen.add("degrees_for_attenuation", double_t, 0, "Angular sector of the point cloud considered when computing the attenuation (in degrees)", 10.0, 5.0, 180.0)
gen.add("K_wall_repulsion", double_t, 0, "Speed in m/s for the repulsion after penetrating 1 m in the forbidden area regarding walls", .0, 0.0, 3.0)

gen.add("min_distance_ceiling", double_t, 0, "Minimum distance allowed from the ceiling", .5, 0.5, 10.0)
gen.add("attenuation_distance_ceiling", double_t, 0, "Distance from the ceiling in meters to start attenuating the speed", .5, 0.5, 10.0)
gen.add("K_ceiling_repulsion", double_t, 0, "Speed in m/s for the repulsion after penetrating 1 m in the forbidden area regarding the ceiling", .0, 0.0, 3.0)

gen.add("max_height", double_t, 0, "Maximum height allowed", 4.0, 2.0, 30.0)
gen.add("attenuation_max_height", double_t, 0, "Height in meters to start attenuating the vertical speed", 3.0, 1.0, 30.0)
gen.add("K_max_height_attraction", double_t, 0, "Speed in m/s for the attraction to the ground after trespassing 1 m the maximum height allowed", .0, 0.0, 3.0)

exit(gen.generate(PACKAGE, "srv_mav_behaviours", "safety_manager"))
